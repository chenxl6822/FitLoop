server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    resolver 127.0.0.11 ipv6=off valid=30s;
    set $backend_origin http://backend:8080;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location = / {
        try_files /download.html =404;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    location = /download {
        try_files /download.html =404;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    location = /download.html {
        try_files /download.html =404;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    location = /apk/version.json {
        root /usr/share/nginx/html;
        try_files /apk/active/current/version.json =404;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    location = /apk/app-release.apk {
        root /usr/share/nginx/html;
        try_files /apk/active/current/app-release.apk =404;
        etag on;
        add_header Cache-Control "no-cache" always;
    }

    location = /apk/app-release.apk.sha256 {
        root /usr/share/nginx/html;
        try_files /apk/active/current/app-release.apk.sha256 =404;
        default_type text/plain;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    }

    location /apk/ {
        return 404;
    }

    location /api/ {
        proxy_pass $backend_origin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass $backend_origin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location /actuator/health {
        proxy_pass $backend_origin;
    }
}
