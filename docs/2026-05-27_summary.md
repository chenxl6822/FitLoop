# FitLoop 项目日总结 — 2026-05-27（周三）

> 从零 MVP 到完整项目规划，今日完成 6 次提交（+3874/-55 行），新增 50+ 文件。
> 项目从 3 个 Flutter 文件膨胀到 9 个 Flutter 文件 + 完整的 iOS 工程 + 后端统计 API。

---

## 一、今日总览

| 指标 | 值 |
|------|-----|
| 提交次数 | 6 次（`683c4c9` → `b903b09`） |
| 新增文件 | 50+（含 iOS 工程文件 40+） |
| 新增代码 | **+3,874 行** |
| 删除代码 | -55 行 |
| 签入时间 | 17:48 → 21:49（约 4 小时） |
| Flutter analyze | 0 issues ✅ |
| Flutter test | 9/9 pass ✅ |

### 项目规模变化

```
                 今日前                 今日后
Flutter .dart:   3 个                  9 个
Widget 测试:     8 个                  9 个
后端 Java:      ～360 行 Stats 新增   +27 行 静态资源 +70 行 头像上传
iOS 工程:       无                    完整 Xcode 工程
后端测试:       43 个 @Test           47 个 @Test
```

---

## 二、今天的 6 个提交（按时间倒序）

### 1️⃣ `b903b09` — 启动页 + 引导页（Task 9）

**新增文件：** `mobile/lib/splash_screen.dart`、`mobile/lib/onboarding_screen.dart`

**修改文件：** `mobile/lib/main.dart`、`mobile/test/widget_test.dart`

**代码作用：**
- **`splash_screen.dart`**（87 行）— 启动页，显示 Logo + 3 秒动画淡入，自动检查首次启动，首次进入引导页，非首次直接跳主页
- **`onboarding_screen.dart`**（166 行）— 三页引导轮播（功能简介：运动打卡 / 校园社交 / 数据统计），带圆点指示器 + "跳过" + "开始使用"按钮
- **`main.dart` 改动** — 新增 `_AppEntry` widget，替换原来 `AuthGate` 直接作为根路由。逻辑：`SplashScreen → OnboardingScreen（如果首次）→ AuthGate（登录/主页）`
- **`widget_test.dart`** — 增补引导页测试 + 启动页测试

**用户价值：** 每次 App 启动不再直接白屏跳转，取而代之的是品牌展示（Logo 动画）→ 功能引导（仅首次）→ 无缝进入应用。降低了新用户的理解门槛。

---

### 2️⃣ `578297e` — Android/iOS 权限配置（Task 11）

**修改文件：** `AndroidManifest.xml`、`Info.plist`、iOS 完整工程文件

**新增文件：** 整个 `mobile/ios/` 目录（Flutter 生成的 iOS 原生工程，40+ 文件）

**代码作用：**
- **`AndroidManifest.xml`** — 新增权限声明：
  - `CAMERA` — 拍照打卡、头像拍照
  - `READ_MEDIA_IMAGES`（Android 13+）— 读取照片
  - `READ_EXTERNAL_STORAGE`（Android 12-）— 旧版兼容
  - `POST_NOTIFICATIONS`（Android 13+）— 运动/喝水提醒
- **`Info.plist`** — 新增 iOS 权限提示文案：
  - `NSCameraUsageDescription` — "FitLoop needs camera access to take profile photos and sport check-in photos"
  - `NSPhotoLibraryUsageDescription` — 访问相册
- **iOS 工程文件** — 完整的 Xcode project，为后续 iOS 构建奠定基础

**用户价值：** 没有这些权限声明，image_picker（拍照）和 flutter_local_notifications（提醒）在 Android 8+/iOS 上会直接闪退或者静默失败。现在 App 能主动弹窗请求权限了。

---

### 3️⃣ `b12bdfc` — 头像上传 UI（Task 8）

**修改文件：** `mobile/lib/api_client.dart`、`mobile/lib/local_cache.dart`、`mobile/lib/main.dart`、`mobile/pubspec.yaml`、`mobile/pubspec.lock`

**新增文件：** `task8_avatar_ui_spec.md`（规范文档）

**代码作用：**
- **`api_client.dart`**（+34 行）— 新增 `uploadAvatar()` 方法，使用 `http.MultipartRequest` 发送文件到 `/api/user/avatar`
- **`local_cache.dart`**（+3 行）— 新增 `avatarUrl` 缓存，登录时不需额外请求
- **`main.dart`** — 个人中心页面增加圆形头像组件，点击触发 `image_picker`（拍照 / 从相册选），上传后实时回显
- **`pubspec.yaml`** — 新增 `image_picker` 依赖
- **`widget_test.dart`** — 测试头像组件渲染

**后端配合**（今日稍早的 `683c4c9` 提交）：
- **`AvatarController.java`**（70 行）— `POST /api/user/avatar`，接收 multipart 文件，校验扩展名（jpg/png/gif），写入 `uploads/avatars/` 目录，返回 URL
- **`StaticResourceConfig.java`**（27 行）— 将 `/uploads/**` 映射为静态资源路径
- **`UserService.java`** — 存头像 URL 到用户表

**用户价值：** 用户现在可以自定义头像了——从个人资料页点击头像 → 拍照或选照片 → 自动上传更新。没有头像时显示灰色占位图标。

---

### 4️⃣ `386f5aa` — 修复离线同步队列（Task 3.3 修正）

**修改文件：** `mobile/lib/connectivity_service.dart`、`mobile/lib/local_cache.dart`、`mobile/lib/main.dart`、`mobile/lib/sync_queue.dart`

**代码作用：**
- **`sync_queue.dart`**（173 行）— 修复：`TrackPoint` 字段名 `latitude` → `lat`、`longitude` → `lng`，`accuracy` 改为非空。确保反序列化正确。
- **`connectivity_service.dart`**（70 行）— 修复：`HttpClient().getUrl()` 缺少 `await`，网络探测一直返回 false
- **`main.dart`** — 修复 `TokenStorage.load()` → `LocalCache.loadToken()` 的引用
- **`local_cache.dart`** — 调整字段匹配

**用户价值：** 没有这个修复，离线打卡队列断网时会把同步失败的信息悄悄吞掉，用户以为打卡成功但实际上没发到服务器。现在断网 → 存队列 → 恢复网络 → 自动重传，完全无感。

---

### 5️⃣ `683c4c9` — 历史统计 API + 头像上传后端（Task 5 + Task 8 后端）

**新增文件：** `backend/.../StatsDtos.java`、`StatsService.java`、`StatsHistoryTest.java`、`AvatarController.java`、`StaticResourceConfig.java`

**修改文件：** `SportRecordRepository.java`、`StatsController.java`、`UserService.java`

**代码作用（按功能分组）：**

#### 【API 层 — 运动历史统计】
| 文件 | 行数 | 作用 |
|------|------|------|
| `StatsDtos.java` | 19 行 | 定义 `SportHistoryResponse` DTO，含日期/次数/时长/距离/卡路里 |
| `StatsService.java` | 88 行 | 查询最近 N 天每日运动汇总，按日期分组聚合 duration/distance/calorie |
| `StatsController.java` | 15 行 | `GET /stats/history?days=7` 接口 |
| `SportRecordRepository.java` | +13 行 | `@Query` 按用户+日期范围分组统计 |
| `StatsHistoryTest.java` | 114 行 | 4 个单元测试 @DataJpaTest（实测数据库查询逻辑） |

#### 【API 层 — 头像上传】
| 文件 | 行数 | 作用 |
|------|------|------|
| `AvatarController.java` | 70 行 | `POST /api/user/avatar` 文件上传端点，校验扩展名，写磁盘，返回 URL |
| `StaticResourceConfig.java` | 27 行 | WebMvcConfigurer：把 `/uploads/**` 映射为可访问的静态资源 |
| `UserService.java` | +6 行 | `updateAvatar(userId, file)` 保存文件 + 更新用户表 avatar_url 字段 |

**用户价值：** 
- 历史统计 API 喂饱了统计页的图表——之前图表用的是本地伪造的分布数据，现在从服务器拉真实数据
- 头像上传 API 供 App 调用来保存用户头像图片

---

### 6️⃣ `b4b0318` — 合并 feature/charts 分支（Task 4+5+6+7）

**来源分支：** `feature/charts`（包含图表 + 提醒 + 社交 + 申诉）

**这是今天所有功能的基础合并**。在此之前项目只有 GPS 打卡 + 运动结果页 + 基础测试。

**合并带来的功能（1404 行新增）：**

| 功能 | 文件 | 行数 | 用户价值 |
|------|------|------|---------|
| **📊 运动图表** | `stats_charts.dart` | 319 | 可视化展示运动时长、里程、卡路里趋势——一眼看出这周运动够不够 |
| **🔔 本地通知** | `reminder_scheduler.dart` | 105 | 运动提醒、久坐提醒、喝水提醒、睡眠提醒——到了时间自动弹通知 |
| **👥 好友列表** | `main.dart` | ~80 | 搜索添加好友、查看好友运动数据——社交动力 |
| **🏆 排行榜** | `main.dart` | ~60 | 按运动时长/里程排名——竞赛激励 |
| **🏅 勋章** | `main.dart` | ~40 | 达到里程碑自动获得勋章——成就系统 |
| **📋 申诉** | `main.dart` | ~100 | 异常记录申诉、管理员审核——用户权益保障 |
| **📝 数据录入** | `main.dart` | ~120 | 体重/睡眠/饮食手动录入——完整健康档案 |
| **🎯 目标管理** | `main.dart` | ~60 | 设置周/月运动目标，进度条可视化——目标驱动运动 |

**影响 Range：** 这个合并之后，main.dart 从 400 行膨胀到 900+ 行，但这之后又经过了 avatar/splash/sync 等改动，最终达到 2400 行。

---

## 三、新增模块详解（按文件夹）

### mobile/lib/ — Flutter 端（9 个文件）

| 文件 | 行数 | 作用 | 依赖 |
|------|------|------|------|
| `main.dart` | 2400 🏋️ | 全部 UI 页面（10+ 个页面）+ 路由 + AuthGate + AppShell | 所有模块 |
| `api_client.dart` | 1070 | 所有后端 API 调用封装（20+ 接口），含 JWT 自动携带、错误处理 | `dart:io` HttpClient |
| `stats_charts.dart` | 319 | 运动历史图表组件（时长/里程/卡路里/体重 4 个面板） | `fl_chart` |
| `sync_queue.dart` | 173 | 离线打卡队列：失败 → 存 SharedPreferences → 网络恢复 → 自动重传 | `shared_preferences` |
| `onboarding_screen.dart` | 166 | 首次启动 3 页引导轮播 | 无 |
| `local_cache.dart` | 135 | Token 缓存 + 目标/统计/历史/体重数据缓存 + 新鲜度判断 | `shared_preferences` |
| `reminder_scheduler.dart` | 105 | 本地通知调度（运动/久坐/喝水/睡眠） | `flutter_local_notifications` |
| `splash_screen.dart` | 87 | Logo 淡入动画启动页 | 无 |
| `connectivity_service.dart` | 70 | 网络连通性探测（HTTP 请求无头检测，非 wifi/cellular 状态） | `dart:io` |

**核心数据流图：**

```
[用户操作] → main.dart(UI) → api_client.dart(HTTP) → [服务器]
                              ↑
[离线] → sync_queue.dart(SP队列) → 恢复网络 → 自动重试 → api_client → 服务器
         ↑
         connectivity_service.dart(网络探测)
         
[cache] → local_cache.dart(SP缓存) → main.dart(UI直接读) → 避免重复请求

[schedule] → reminder_scheduler.dart → 系统通知
```

### backend/ — Java 端（6 个新增/修改文件）

| 文件 | 行数 | 作用 |
|------|------|------|
| `StatsService.java` | 88 | 运动历史数据聚合服务 |
| `AvatarController.java` | 70 | 头像文件上传控制器 |
| `StatsHistoryTest.java` | 114 | 统计服务数据库集成测试 |
| `StatsDtos.java` | 19 | 响应数据 DTO |
| `StaticResourceConfig.java` | 27 | 图片静态资源路径映射 |
| `StatsController.java` | 15 | 统计查询 REST 端点 |

### docs/ — 新建文档

| 文件 | 内容 | 用途 |
|------|------|------|
| `OPENCLAW_HANDOFF.md` | 完整开发上下文 | 给后续 AI 会话的连续交接文档 |
| `STATUS.md` | 项目状态 | 快速查看所有任务进度 |
| `CLAUDE_CONTEXT.md` | 项目概述 | 给 Claude 的全局上下文 |
| `PROJECT_PLAN_v2.md` | 完整规划 | 7 阶段 ~55h 规划 + 腾讯云部署方案 |
| `task8_avatar_ui_spec.md` | 头像上传规范 | Claude 执行 Task 8 的详细指令 |
| `task9_splash_onboarding_spec.md` | 启动页规范 | Claude 执行 Task 9 的详细指令 |
| `task11_permissions_spec.md` | 权限配置规范 | Claude 执行 Task 11 的详细指令 |
| `task12_backend_tests_spec.md` | 测试补充规范 | Claude 执行 Task 12 的详细指令 |

---

## 四、今日整体意义

### 4.1 「从功能原型→准生产应用」

| 之前 | 之后 |
|------|------|
| 3 个 Dart 文件，仅核心打卡 + 登录 | 9 个 Dart 文件，10+ 个功能页面 |
| 无 iOS 支持 | 完整 iOS Xcode 工程 |
| 无图表 | 运动/体重趋势可视化 |
| 无通知 | 4 类智能提醒 |
| 无社交 | 好友/排行榜/勋章 |
| 无离线 | 中奖队列 + 自动同步 |
| 无头像 | 完整上传 + 展示 |
| 无启动体验 | Logo 动画 + 引导轮播 |
| 无权限声明 | 全平台权限合规 |
| 无测试 | 9 个 widget test + 47 后端 @Test |

### 4.2 技术债务

- **main.dart（2400 行）** 已经是单体累赘——所有页面挤在一个文件，后续应拆分
- **多运动类型未支持** — 当前只写了 `"running"` 硬编码
- **iOS 构建未验证** — 工程文件完整但未用 macOS 编译验证
- **头像存在本地磁盘** — 生产应用应该改为 COS/OSS 对象存储

### 4.3 下一阶段建议

```mermaid
graph LR
    A[补多打卡方式] --> B[腾讯云部署]
    B --> C[社交增强]
    C --> D[第三方登录]
    D --> E[附加功能]
```

1. **补全 4 种打卡方式**（传感器/拍照/手动）— 最大功能缺口
2. **部署到腾讯云** — 尽早暴露线上问题
3. **社交 + 第三方登录** — 提升用户粘性

---

*文档生成：2026-05-27 15:05 UTC*
*基于 `887af38..b903b09` 的 6 次提交分析，累计 69 文件变化（+3874/-55）*
