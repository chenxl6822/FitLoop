# FitLoop Android 真机冒烟清单

适用于 `0.1.7+8` 短期受控 HTTP 测试候选。项目负责人必须事先接受登录、
会话令牌和 API 数据通过 `http://43.139.72.25` 明文传输的风险。自动化
门禁通过后，使用该精确端点、真实服务器和至少一台 Android 真机执行；
任何阻塞项都不得标记为通过。第 3、6 项是 APK 原子激活后的发布验证，
其余 31 项和下方 AI 教练专项验收必须在激活前通过。本清单不构成 HTTPS 或安全生产验收。

## 发布记录

| 项目 | 记录 |
| --- | --- |
| App 版本 / versionCode | `0.1.7 / 8` |
| Git commit | |
| APK SHA-256 | |
| APK 签名证书 SHA-256 | |
| API 地址与传输模式 | `http://43.139.72.25` / 临时明文 HTTP |
| HTTP 风险批准人 / 时间 | |
| 受控测试开始 / 结束时间 | |
| 旧版安装包版本 | 线上可信 `0.1.5+6`（若确有已分发且已核验的 `0.1.6+7`，另补测） |
| 测试设备 / Android 版本 | |
| 测试人 / 日期 | |
| HTTP 兼容窗口开始/结束日期 | |

## `0.1.7+8` AI 教练专项验收

| 检查项 | 预期结果 | 结果 |
| --- | --- | --- |
| 确认训练计划后立即查看 | “查看计划”可打开新建计划，目标和每日安排完整显示 | |
| 退出并重新进入 AI 教练 | “我的计划”仍能加载该计划并打开详情 | |
| 计划所有权隔离 | 当前用户只能看到自己的计划，其他账号不可读取 | |

## 自动化门禁

```powershell
cd backend
mvn --batch-mode --settings ../.github/maven-settings.xml verify

cd ../agent-service
python -m compileall -q src tests
python -m pytest

cd ../mobile
flutter analyze
flutter test
cd ..
powershell -ExecutionPolicy Bypass -File deploy\build-apk.ps1 `
  -ApiBaseUrl 'http://43.139.72.25' `
  -SigningMode Compatibility `
  -AllowInsecureHttpTransitionRelease
```

具备 Docker 的 CI 还必须通过 Testcontainers 集成测试和基础/TLS Compose 配置校验。

## 服务、传输决策与安装

| # | 检查项 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 1 | HTTP 过渡风险批准和端点精确性 | 已记录批准人、测试期限和明文风险；APK 与 `version.json` 只使用 `http://43.139.72.25` | |
| 2 | HTTP `/actuator/health` | `http://43.139.72.25/actuator/health` 返回 `{"status":"UP"}` | |
| 3 | 激活后的 `/apk/version.json` 与 APK SHA-256 | 公网版本、versionCode、哈希与发布记录一致 | |
| 4 | 全新安装 `0.1.7+8` | 安装成功并正常启动 | |
| 5 | 从线上可信 `0.1.5+6` 覆盖升级；若确有已分发且已核验的 `0.1.6+7`，再补做该版本升级 | 无签名冲突，登录数据和用户数据保留 | |
| 6 | APK 原子回滚演练 | 可通过 `active/previous` 重新激活上一组三件套，哈希和版本元数据匹配 | |

## 认证生命周期与资料

| # | 检查项 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 7 | 邮箱验证码注册 | 邮件可收到，验证码一次性有效 | |
| 8 | 密码登录、验证码登录、重置密码 | 三条链路均成功且提示正确 | |
| 9 | access token 即将过期时继续操作 | 客户端主动刷新，页面和请求不中断 | |
| 10 | access token 失效后并发发起多个请求 | 只发生一次 refresh，原请求最多重放一次且均正确结束 | |
| 11 | 服务端撤销 refresh token 后继续请求 | 清除本地会话并返回登录页，不循环刷新 | |
| 12 | 离线重启已登录 App，再恢复网络 | 离线期间不误删可刷新会话；恢复后可刷新或明确要求登录 | |
| 13 | 主动退出后重启 App | refresh token 被撤销，本地安全存储已清理 | |
| 14 | 从旧版仅 access token 会话升级 | 不使用不安全旧会话，明确要求重新登录 | |
| 15 | 上传 JPG/PNG 头像 | 上传成功，重新进入后仍显示 | |

## 核心业务

| # | 检查项 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 16 | GPS 打卡开始、暂停、继续、结束 | 记录成功，距离和时长合理 | |
| 17 | 计步、拍照、手动打卡 | 三种模式均可创建运动记录 | |
| 18 | 目标创建、进度刷新、编辑、删除 | 列表和统计立即更新 | |
| 19 | 健康数据与统计图表 | 数据保存且趋势显示正确 | |
| 20 | 好友搜索、添加、列表、排行榜 | 数据可加载，异常有友好提示 | |
| 21 | 异常记录申诉和管理员审核 | 提交、查看、处理和审计链路正常 | |
| 22 | Agent 申诉审核 | readiness 正常时能生成提案，确认/拒绝可审计 | |

## 提醒设置

| # | 检查项 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 23 | 开启运动、久坐、喝水、睡眠提醒 | 保存成功，个人页状态立即更新 | |
| 24 | 修改时间和每日/每周/单次周期 | 旧通知被替换，不产生重复通知 | |
| 25 | 关闭任意一类提醒 | 只取消该类型，不影响其他类型 | |
| 26 | 拒绝通知权限后保存 | 不闪退，提示前往系统设置，服务端状态不误改 | |
| 27 | Release 包反复开关提醒并重启 | 无 `Missing type parameter`，状态与保存结果一致 | |

## 网络、降级与恢复

| # | 检查项 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 28 | 飞行模式下结束运动 | 请求进入离线队列，App 回到可用状态 | |
| 29 | 网络恢复后同步离线队列 | 同步成功且不重复生成记录 | |
| 30 | 弱网上传头像或运动照片 | 超时有提示，页面不锁死 | |
| 31 | 停止 Agent 或使 DeepSeek 不可用 | 登录、运动、管理核心接口和 APK 下载仍正常；仅 Agent 告警 | |
| 32 | 服务端返回 401/403/500 | 刷新规则正确，最终错误可理解且无无限重试 | |
| 33 | 新 APK 使用获批的精确 HTTP 过渡端点 | APK 与 `version.json` 只指向 `http://43.139.72.25`；尾斜杠、端口、路径、查询、片段、其他 HTTP 地址及无开关构建均被拒绝 | |

## 发布结论

| 项目 | 记录 |
| --- | --- |
| AI 教练专项通过数 | / 3 |
| 激活前既有检查通过数 | / 31 |
| 激活后检查通过数 | / 2 |
| 总通过数 | / 36 |
| 阻塞问题 | |
| 未执行项目及原因 | |
| 是否允许公开 Release | 是 / 否 |

AI 教练专项验收和激活前 31 项全部通过后才能申请 APK 激活批准。激活后立即执行第 3、6 项；
任一失败都必须回滚且不得公开 Draft Release。只有 36 项全部通过，且邮件
验证码、升级签名、回滚和 Agent 降级无阻塞问题时，才允许向受控测试用户
公开 Release。发布记录必须明确注明“短期 HTTP 测试版”，不得宣称 HTTPS
或安全生产验收已经完成。
